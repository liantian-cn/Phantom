# 运行时性能

当前循环按新帧驱动，频率上限与生命周期以 [架构](architecture.md#截图基础运行边界) 为准。

## 分配与缓存

- 复用 NumPy views；仅在字节稳定性要求明确时创建连续副本。
- 缓存静态布局、已验证 AST、插件描述和模板，不缓存超过插件新鲜度契约的战斗值。
- 避免每帧重复建表和解析，诊断输出不放在高频路径。

## 验证

验证裁剪、比例、hash、非线性编码、兜底、首条命中及键位等业务结果；不为固定输出文字本身写脆弱测试。详见 [测试规则](testing.md)。
