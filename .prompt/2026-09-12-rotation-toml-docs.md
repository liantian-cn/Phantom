# Primary

现在项目构架书里，说，未来的循环配置格式是yaml吧。那是前期调研不精，我现在认为toml更好，在还没开始写代码之前，帮我改了文档。

# Question

**修改范围**

> 是否将这 6 份现行文档统一改为 TOML，保留现有字段和循环语义，并将 YAML 视为已废弃的前期设计，不安排兼容或迁移？转换完整示例及关联措辞，历史任务档案保留原文。

统一改为 TOML。

**计划确认与实施授权**

> 是否按已展示的计划更新 6 份文档、验证 TOML 示例，并将本次改动和工作流档案单独提交？

Implement the plan.
