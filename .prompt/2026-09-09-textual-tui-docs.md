# Primary

修改项目文档。我后续不打算使用pyside6了，而是打算把项目的ui做成tui的，textual很成熟，能实现信号之类的效果，对么？

# Question

**文档深度**

> 本次改为 Textual TUI，文档需要确定到什么程度：仅替换选型、补充能力说明，还是同时设计页面、交互和后台任务通信架构？

选型及能力说明。

**既有交互**

> 现有“rotation 表格首列复选框”和“不参考旧项目 Terminal 界面”的规定如何处理？同职业同专精互斥选择的业务规则继续保留；可以保留两项规定、仅将展示形式重新待定，或将两项均重新待定。

保留两项规定。

**Plan implementation authorization**

> Do you authorize implementation of the agreed Textual TUI documentation plan, including its workflow archives and atomic local commit?

Implement the plan.
