# 插件职责与开发手册修正

## 职责与对象设计

1. **R1**：我要修正此前 todo_list 第 7–9 部分实现中的七项问题，并举一反三处理现有实现中的同类问题，不扩展新业务插件。
2. **R2**：我要把条件基类和注册器等公共设施迁到 phantom/core/condition/，phantom/conditions/ 只保留插件。
3. **R3**：我要把参数校验和业务解码分别做成基于基础类的对象，由插件组合使用；基础类及通用实现放独立文件，不放在 base.py。spell_ids、cooldown 等业务规则归具体插件。
4. **R4**：我要同样整理截图目录，把公共设施迁到 core/capture，captures 只保留插件。

## 配置、代码与文档

5. **R5**：我要通过 capture.plugin 在启动时选择精确版本截图插件，默认 gdi@1.0；本次不增加新后端或运行期切换。
6. **R6**：我要补充插件 Python 的业务注释，并纠正复制错误和过期描述。
7. **R7**：我要把 Lua 插件的参数、坐标、宽度、刷新间隔、固定技能 ID、数量上限和编码节点集中在头部声明；构造器引用这些变量，不直接写模板坐标。运行状态保留合理初始化时机，临时变量保留在函数内。
8. **R8**：我要新建 .plugin-development/，以中文 README.md 为总则，将插件开发规则迁入手册；.spec 保留系统契约并链接手册，AGENTS.md 增加路由。

## 兼容与交付

9. **R9**：我要保留现有八个 @1.0 插件的参数、编解码和兜底语义，统一更新仓库导入路径，不保留旧模块。
10. **R10**：我要让旧应用配置缺少截图后端字段时默认使用 GDI，同时保持原配置文件不被自动改写。
11. **R11**：我要实施已确认的计划，完成对应验证和工作流要求的本地提交。
