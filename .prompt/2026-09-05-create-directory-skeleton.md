# Primary

~~~~text
根据项目文档，为我创建目录。不用写代码，空目录用.gitkeep文件占着。
~~~~

# Question

~~~~text
计划草案已创建：[create-directory-skeleton.md](/workspaces/phantom-5796e240/.plan/2026-09-05-create-directory-skeleton.md)。

共享理解如下：

- 创建 `phantom/ui/`
- 创建 `phantom/core/`
- 创建 `phantom/conditions/`
- 创建 `phantom/actions/`
- 创建 `phantom/captures/`
- 创建 `scripts/`
- 创建 `rotations/`
- 每个叶子目录仅放置一个空 `.gitkeep`
- 不创建 `main.py`、Python 文件或具体版本插件目录
- 不改动或提交现有的 `.gitignore`、`LICENSE`、`README.md` 用户变更
- 完成验证后创建一个原子本地提交，不推送

❓ **Q1 — 最终确认**：请选择下一步。

1. **确认并授权实施（Recommended）**
2. **修改计划**

➡️ 推荐选择：**1**
~~~~

~~~~text
1
~~~~
