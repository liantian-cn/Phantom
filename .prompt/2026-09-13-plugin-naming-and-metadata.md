# 插件命名与版本

- **R1**：我要将现有 8 个条件插件和 GDI 截图插件统一改为 `liantian_cn.插件名@dev`。作者名应符合 Python 包名要求，当前作者 `liantian-cn` 使用 `liantian_cn`。
- **R2**：我要仅通过 Agent 规则约束命名与版本。数字或点分数字版本是正式版，接口参数和逻辑不能修改；包含测试标识的版本允许修改。加载器仅保留路径安全、精确加载和接口检查。
- **R3**：我要同步修改 `blood-dk.toml` 和仓库当前引用，直接迁移，不保留旧标识兼容；历史档案保持原文。

# 主入口

- **R4**：我要将 `rotations.main` 改为 `phantom.main`，同步相关引用，不保留旧入口兼容。

# 插件自述

- **R5**：我要为现有每个插件添加给 AI Agent 阅读的 `plugin.toml`，以后通过软规则要求编写，Python 代码不读取它。
- **R6**：自述要包含完整插件标识、由 Agent 生成的中文名字、配置参数与返回值说明，以及中等详细的用途简介，说明主要 API 名称、转换关系、返回类型，不介绍 API 的具体用法。
- **R7**：条件插件要提供推荐条件名，`spell_cooldown` 使用 `{技能名称}的冷却时间`，其他名称由 Agent 根据业务含义生成。
- **R8**：GDI 自述按截图职责说明 `fps`、图像与状态返回、主要 Windows API 和颜色转换，推荐条件名标注“不适用”。
