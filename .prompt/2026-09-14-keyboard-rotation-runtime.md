# Primary

帮我完成todo_list的第14-16步

## 第14步

- 如何发送按键可参考 https://raw.githubusercontent.com/liantian-cn/EZWowX2/refs/heads/main/Terminal/terminal/keyboard.py
- 上面的游戏是我自己写的，所以首个PostMessageW插件参考是最好的。

问题在"Action"这个名字
- 我是希望，phantom内核决定发送什么按键，比如是CTRL+1还是ALT+F4
- 插件只负责发送按键，不负责解释宏 or 其他什么。
- 现在做PostMessageW，未来可能SendMessageW，还可能调用驱动，还可能串口仿真。但无论如何，插件只是不同形式的处理按键。
- 那么插件目录还叫actions似乎很不合适，先帮我想法名字


## 第15步

目前没有特别的想法，但是当前没有测试环境


## 第16步

似乎已经完成。

# Question

**插件命名**

> 按键发送插件采用哪个名称？keyboards 对应中文键盘插件、core/keyboard 和 keyboard.py。

keyboards

**插件化范围**

> 第 14 步是否直接实现精确版本加载，将第 17 步的按键插件化一并提前完成？

直接插件化

**验收边界**

> 当前没有游戏测试环境，本次第 14–16 步按哪种交付边界推进？

实现并离线验收

**调度方式**

> 持续运行采用哪种节奏？新帧驱动表示每个新截图最多决策并发送一次，同一帧不重复发送，只用最新帧、不补跑积压任务。

新帧驱动

**目标窗口**

> 首版如何确定发送目标？现有项目识别 _retail_/wow.exe；你提到的自写游戏是否需要作为额外运行目标接入？

不同插件，不同的目标。如果是驱动模拟，就没有目标了。现在的目标，写死在插件里，目标是“魔兽世界”这个窗口。

**TUI 启停**

> TUI 如何启动真实发键？现有启动即运行表示配置有效 rotation 时启动采集与持续执行，无 rotation 时保持仅采集，关闭统一停止。

现有启动即运行

**状态门控**

> 是否采用截图有效、职业专精匹配、Lua 启用为白色且延迟为黑色才发送的条件，不满足时只跳过当前帧，恢复后自动继续，爆发不增加全局门控？

没搞懂讨论这个的意义，只有rotation匹配到宏，宏匹配到按键，才会发送不是么。如果宏没按键（Sleep or Pass）都不执行。你的问题没有意义，你说这些情况，rotation运行并匹配了么？问题是你问出这个问题，那证明对逻辑和概念理解有误吧。

**失败处理**

> 找不到目标窗口、发送失败或执行异常时，持续运行如何处理？截图暂时无效仍按现有方式跳过并等待恢复。

暂停并手动重启

**运行前提**

> Lua 启用和延迟状态应如何影响内核运行 rotation？现有代码尚未实现这部分。

放进表达式

**无按键动作**

> 你提到 Sleep／Pass，本次需要怎样支持无按键动作？

Sleep Pass未来作为Idle的别名，都是跳过当前循环一次。

**通用变量名**

> 启用、爆发、延迟作为内置布尔变量进入表达式，采用哪套名称？这些名称将保留，禁止条件插件标题重名。

中文名称

**示例循环**

> 是否在血 DK 示例 rotation 顶部增加“未启用或正在延迟时命中 Idle”的规则？

增加规则

**窗口匹配**

> PostMessageW 插件内部如何匹配写死的“魔兽世界”窗口？精确匹配要求唯一同名窗口，否则报错暂停。

标题精确匹配

**宏绑定标准示例**

> 宏绑定生成应参考哪个标准示例？

你是否在找怎么绑定宏，https://raw.githubusercontent.com/liantian-cn/EZWowX2/refs/heads/main/DejaVu/DejaVu_DeathKnight/Blood/Macro.lua是标准示例。

**键位范围**

> 首版键位支持范围如何确定？仍使用现有大写连字符格式，例如 CTRL-1、ALT-F4。常用键盘键包括字母、数字、F1–F24、小键盘、方向与编辑键、常用标点及 CTRL/ALT/SHIFT 组合。

常用键盘键

**实施确认**

> 是否按完整方案实施键盘插件、宏绑定、新帧运行、表达式变量及离线验收，并保留真实游戏待验？

Implement the plan.
