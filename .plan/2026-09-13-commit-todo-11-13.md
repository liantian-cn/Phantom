## Goal

Commit all current changes for todo_list sections 11–13 after fixing the two observed TUI test failures.

## Scope

All 14 existing modified or untracked files, including the author ignore rule in .gitignore, plus the necessary TUI shutdown fix, regression test, verification documentation and workflow archives. Local develop branch only.

## Decisions

- Include all current changes, as explicitly selected by the user.
- Fix the two failing TUI tests before committing, as explicitly selected by the user.
- Preserve expression evaluation, standalone demo and automatic TUI decisions; add no public interfaces or action execution.
- Use one atomic local commit; do not push.

## Implementation Steps

1. Diagnose and fix capture refresh accessing decision widgets during UI teardown using the existing application lifecycle.
2. Add a deterministic shutdown regression and verify existing capture, decision and clearing scenarios.
3. Run full pytest, strict type checks, Ruff checks and git diff --check; record actual results.
4. Inspect and stage all agreed files and workflow archives, commit, and verify repository status and commit contents.

## Acceptance Criteria

- Both observed TUI failures are resolved, and refresh during shutdown cannot access removed widgets.
- Full automated checks pass and existing decision behavior is preserved.
- All agreed changes and archives are included in one local commit on develop.

## Verification

- Before implementation: pytest reported 273 passed and 2 failed (test_valid_failed_and_paused_data_never_reuse_old_frame and test_capture_termination_pauses_and_explicit_restart_works); both failed with NoMatches for #decision during refresh. Strict types, Ruff check, format --check and diff whitespace checks passed.
- Archive collision checks found no same-named plan, mean or prompt. None of these paths is ignored by Git.
- The two new deterministic shutdown cases reproduced NoMatches on the original implementation. With the public is_running guard, all 277 pytest cases passed in 11.98 seconds, including both original failures and both shutdown cases. The latter verify capture cleanup and no remaining phantom threads.
- scripts/check_types.py passed for the core and every versioned plugin entry. Ruff check passed. Ruff format normalized only the edited Python files' line endings; the final format check passed for all 59 files, and git diff --check passed before staging.
- Existing game verification records were preserved; no live game verification was repeated for this commit task.

## Review Notes

- 2026-09-13T20:05:24+08:00: Recorded confirmation from the user's “Implement the plan.” message following the native plan flow and the switch to Default mode. Goal, Scope, Decisions, Implementation Steps and Acceptance Criteria are frozen. No files were written in Plan mode.
- Inspection of installed Textual shows _shutdown sets is_running false before pruning screens, while Phantom sets closing in the later on_unmount callback. The refresh guard will use the public is_running property.

## Completion

Implementation is complete. All current task changes, the TUI shutdown fix and regression, verification documentation and three archives are prepared for one atomic local commit on develop with message: 完成 todo_list 第 11–13 部分并修复 TUI 退出刷新. The commit containing this record is the delivery artifact; its hash and post-commit status will be reported to the user after Git confirms success.
