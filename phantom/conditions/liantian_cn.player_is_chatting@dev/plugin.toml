name = "liantian_cn.player_is_chatting@dev"
display_name = "玩家是否具有键盘输入焦点"
description = "frame = GetCurrentKeyBoardFocus()；返回输入焦点框体或 nil。EventRegistry:RegisterCallback(event, callback, owner) 注册聊天焦点通知。任意输入框获得焦点即为真，包含搜索框；保留聊天通知和两秒轮询。"
recommended_condition_name = "玩家是否具有键盘输入焦点"

[parameters]
description = "无参数，拒绝多余字段。"

[returns]
type = "bool"
description = "任意输入框获得焦点即为真，包含搜索框；保留聊天通知和两秒轮询。"
fallback = false
