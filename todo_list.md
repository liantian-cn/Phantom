# Phantom 开发路线图

## 目标与使用方式

最终目标是个人完整可用版本：从 rotation TOML 生成游戏插件，通过截图与解码取得状态，按优先级选择动作并发送按键，在 Textual TUI 中查看和控制运行。

“多循环”指管理多份 rotation，支持生成、选择与职业／专精匹配，**同一时刻运行一份 rotation**。单次执行和持续调度另设里程碑。

建议按编号推进；每步达到完成标准后再勾选。第 1 步按用户确认完成，第 2、3 步的实现与验收进展见对应条目。后续步骤仍需依照项目工作流单独明确需求；本路线图不代表所有实现细节已经冻结，也不代表功能已经验证。

项目规则以 [规范入口](.spec/README.md) 为准。本文件记录实施顺序与阶段验收；具体接口、插件清单、TUI 页面和调度策略在对应阶段确认，并按需更新相关规范。

## 第一阶段：先能取得并看见数据

### 1. Lua 基础框架

- [x] 已完成（用户确认）

**目标：** 为 Python 端提供游戏内像素输出基础。

**前置依赖：** 无。

**主要任务：** 已建立共享运行时、Cell、Value Bar、Icon Tile 和通用状态字段；后续以现有源码对接，不重新搭建框架。

**完成标准：** 本项沿用用户的完成确认。Python 接入后的实际截图与编解码一致性由后续步骤验证，此勾选不代替端到端验收。

### 2. Python 工程基础

- [x] 已完成：Python 3.13、pip、最小模块入口和开发检查；纯算法与 Windows 调用分离。

**目标：** 建立后续模块可共同使用的最小开发和运行入口。

**前置依赖：** 第 1 步提供对接基础；本步本身不依赖游戏运行。

**主要任务：** 确定 Python 版本、依赖管理、启动入口、测试与静态类型检查工具；沿用已有目录职责，分清纯算法代码和 Windows 调用边界。

**完成标准：** 能按记录的命令安装依赖、启动最小程序、运行检查；容器能够验证纯 Python 逻辑，Windows 能够运行目标入口。

**已确认设计：** Python 3.13、requirements 依赖文件、pytest、mypy、Ruff；`python -m rotations.main` 预留未来 Textual 入口，本次不连接截图。纯图像测试可跨平台执行，当前验证环境为 Windows。

### 3. 屏幕截图基础模块

- [x] 已完成：用户于 2026-09-12 确认测试成功；第四步游戏 demo 同日取得有效基板并完成解析。

**目标：** 在 Windows 上取得游戏像素画布的原始图像。

**前置依赖：** 第 1、2 步。

**主要任务：** 实现虚拟桌面基板定位、GDI 截图和目标区域提取；返回 RGB NumPy 数组与错误状态；提供独立定时 demo 保存最后结果。

**完成标准：** 在 Windows 上截到正确基板区域，数组尺寸和颜色与画面对应；找不到基板或截图失败时有明确结果，不把失败当成有效图像。

**已确认设计：** 独立后台线程搜索整个虚拟桌面，精确 4×4 角标定位，中心 2×2 校验，默认 FPS 15 且支持运行中设置。实现放在 `gdi@1.0`，通用版本发现与其他截图后端仍留到第 16 步。Lua 保持 DEBUG=true，本次不要求用户运行游戏。

### 4. NumPy 像素解析

- [x] 已完成：PixelDecoder、Cell、ValueBar、IconTile、算法测试与 Windows 游戏 demo 已验证（2026-09-12）。

**目标：** 把截图转换为通用字段及条件区域的原始值。

**前置依赖：** 第 2、3 步；纯算法可使用人工构造图像独立验证。

**主要任务：** 按 [像素协议](.spec/pixel-protocol.md) 实现通用字段读取、Cell 裁剪、Value Bar 百分比和 Icon Tile 空槽／hash；暂不把条件专属业务公式写进通用解析器。

**完成标准：** 测试覆盖有效区域、边缘污染、严格黑白判断、无有效黑白像素、全黑 Icon Tile、多槽位和非连续数组；真实截图中的通用字段能够正确读取。

**已验证结果：** `demo/demo01.py` 定时读取同一帧，第一行 Cell mean 为 `6,1,255,0,0`，第二行为 `255,0,0,0,255`；`getValueBar(1,2)` 的 ratio 为 `1.0`、percent 为 `100.0`；两个 IconTile hash 均为 `None`。非空 hash 与缓存由合成图测试覆盖，本次游戏画面未覆盖非空图标。

### 5. Textual TUI 基础

- [x] 已完成：Textual 界面、工作目录配置、游戏检测、采集生命周期与业务日志已接入（2026-09-12）。

**目标：** 建立能承载采集和后续运行操作的终端界面。

**前置依赖：** 第 2 步；接入采集任务依赖第 3 步。

**主要任务：** 确定最小布局与操作入口，建立后台任务向界面传递状态的方式，处理启动和退出的任务生命周期。

**完成标准：** 界面可启动和退出；后台采集期间仍能响应操作；退出后任务能够结束。

**阶段设计问题：** 已确认页面组织、通信方式、应用配置与游戏检测规则，见 [TUI 规范](.spec/tui.md)；rotation 表格与插件生成留到第 18 步。

**已验证结果：** 2026-09-12 在 Windows 执行 `pytest` 得到 82 passed，`mypy`（strict）、`ruff check` 与 `ruff format --check` 通过；
桌面 smoke 验证真实入口在启动工作目录创建默认 `phantom.toml`，界面以暂停状态打开，未检测到游戏时“启动”禁用，
标签顺序与 Ctrl+Q 退出符合规范，退出后不残留后台线程。真实 GDI 线程在无基板时报告可恢复错误并可正常停止。

### 6. TUI 展示基础采集数据

- [x] 已完成并验证（2026-09-12）

**目标：** 尽早验证“游戏画面 → Python → 用户可见数据”链路。

**前置依赖：** 第 3、4、5 步。

**主要任务：** 展示截图状态、画布信息以及职业、专精、启用、爆发、延迟等已有通用字段；区分当前有效结果与采集失败。

**完成标准：** 在 Windows／游戏内改变可操作状态，TUI 显示随之变化；职业、专精等字段与实际状态一致。这里展示基础数据，条件与决策展示在第 13 步完成。

**已验证结果：** 通用条件页按同一次有效帧读取五个通用 Cell 的 RGB 与纯色 `mean`，暂停或失败时清空旧值；
非纯色亮度与不可用数据使用破折号，显示值列按规范留空。Windows／游戏内状态变化验收已完成，职业、专精、启用、爆发和延迟字段与实际状态一致，界面会随状态变化更新。

## 第二阶段：让配置产生可验证的决策

### 7. Rotation 配置加载与校验

- [x] 已完成：schema、引用与精确插件参数校验，血 DK 示例已验证（2026-09-12）。

**目标：** 把用户 TOML 转换为后续模块可使用的配置数据。

**前置依赖：** 第 2 步。

**主要任务：** 按 [配置规范](.spec/configuration.md) 校验 schema、UUID、职业／专精、条件标题、宏名称及引用关系，建立最小示例；为第 8 步接入精确插件版本和参数校验准备数据。

**完成标准：** 合法配置可加载；版本错误、重名、缺失字段和不存在的引用被明确拒绝。插件参数与表达式的完整校验分别在第 8、11 步补齐。

### 8. 条件插件基础与布局器

- [x] 已完成：独立实例、冻结多区域布局、三列表解码与保留注释的坐标回写已验证（2026-09-12）。

**目标：** 建立从条件配置到冻结像素区域的统一流程。

**前置依赖：** 第 4、7 步。

**主要任务：** 按 [插件规范](.spec/plugin-system.md) 实现精确版本定位、独立实例、参数校验、输出描述、原始值读取、业务解码及兜底；按各行独立布局并冻结输出数量与位置。

**完成标准：** 缺失精确版本和非法参数被拒绝；布局紧密且包含 Value Bar 分隔占位；输出冻结；解码异常返回插件声明的兜底，业务值类型和形状符合契约。

**已确认设计：** 精确目录加载，缓存类而不共享实例，错误附带插件标识；完整契约见插件规范。

### 9. 少量测试条件插件

- [x] 已完成：八个 @1.0 条件插件及 Lua 5.1／Python 离线配对验证（2026-09-12）。

**目标：** 用小规模实例验证条件插件设计，而不是立即扩充完整业务库。

**前置依赖：** 第 8 步。

**主要任务：** 选择少量易于观察和验证的条件，编写 Lua 模板、Python 解码、参数约束与文档；为未被真实条件覆盖的像素类型保留算法测试。

**完成标准：** 参数校验、正常值、边界值、不可用输入和兜底都有验证；Lua 编码与 Python 解码成对一致。游戏内完整链路在第 10 步验收。

**阶段设计问题：** 插件清单、参数与公式在本步确认。涉及 WoW 技术事实时，从 [技术背景入口](.context/README.md) 进入专题并核验指定源码，不能把规范中的示例直接当成已验证实现。

### 10. 单份 rotation 的 Lua 生成器

- [x] 已完成：生成器与离线检查通过，游戏内验收由用户确认有效（2026-09-13）。

**目标：** 首次从真实配置生成可加载的游戏插件。

**前置依赖：** 第 1、7、8、9 步。

**主要任务：** 串联配置校验、条件实例化和布局，生成共享模块、TOC 与 UUID Lua；实现包名校验、职业／专精加载判断和按需条件输出。宏绑定生成在第 14 步补齐。

**完成标准：** 生成产物结构正确，使用到的条件按冻结布局输出；游戏内能加载，并由 Python 解码获得预期值；不匹配的职业／专精不加载该 rotation 的逻辑。

**已验证结果：** 实际生成到 `E:\World of Warcraft\_retail_\Interface\AddOns\Phantom`，23 个文件（含 6 个 media 资源），TOC 引用 16 个 Lua；共享 runtime/general、单个 UUID Lua。所有生成 Lua 经 Lua 5.1 解析。公共冷却独立使用无参数 `spell_gcd@1.0`。

**验收边界：** 本轮共 160 项测试通过，mypy strict（含八个精确版本插件）和 Ruff 检查通过；离线 Lua API doubles 与合成图验证配对，不代表已在游戏中加载或渲染。包标题使用包名，版本等元数据沿用源码 TOC。

**2026-09-13 复验：** 228 项测试、完整类型检查与 Ruff 检查通过。真实 GDI 取得有效 `(20,36,3)` 基板，职业／专精为 `6/1`；八项条件依次为 `0.0, 6, 2, False, 0.0, 0.0, 100.0, False`。采集时安装的 UUID Lua 与当前源码不同，因此这次静态帧不作为当前版本完整验收。随后重新生成 23 个文件，核对文本产物与当前渲染一致，全部 16 个 Lua 通过 Lua 5.1 解析；尚未确认游戏重载。

**用户验收确认（2026-09-13）：** 用户在收到当前产物重载、动态条件变化和职业／专精不匹配的验收步骤后确认“验证有效”，据此将第 10 项标记完成。未提供逐场景记录或实际游戏 build；此前复验记录保留为当时的验证边界。

**游戏验收步骤（保留供回归）：**

1. 使用血 DK 登录，重新加载插件（`/reload`），确认无 Lua 错误；运行 `.venv/Scripts/python -m phantom.main`，启动采集并打开“循环条件”。
2. 核对上述八项条件；手动施法观察符文消耗／恢复、符文能量增减、血沸充能消耗／恢复、公共冷却和死神的抚摩冷却回落。血量变化、高亮触发及灵界打击可用性分别与游戏显示核对；无法触发的场景保留待验，不视为通过。冷却、血量和能量按像素编码精度比较，见插件系统规范。
3. 切换其他专精后 `/reload`，再用其他职业登录，分别确认 UUID Lua 不注册该 rotation 条件逻辑，TUI 条件值为 `—`；返回血 DK 并重载后恢复正常。
4. 记录实际游戏版本、各场景结果和 Lua 错误（如有）；所有场景通过后再勾选第 10 项。

### 11. 条件表达式与优先级求值

- [x] 已完成并验证（2026-09-13）

**目标：** 根据条件业务值选择本轮应执行的宏。

**前置依赖：** 第 7、8、9 步。

**主要任务：** 实现加载时一次性白名单 AST 解析与校验，运行时复用；检查引用、类型及允许的表达式形式；按配置顺序返回首个命中结果，禁止使用 `eval`。

**完成标准：** 测试覆盖允许与拒绝的节点、组合表达式、类型不匹配、首个命中和全部未命中；每轮最多选出一个动作，未命中时无动作。

### 12. 单份 rotation 单次试运行

- [x] 已完成并验证（2026-09-13）

**目标：** 在发送真实按键前核对完整决策链。

**前置依赖：** 第 3、4、10、11 步。

**主要任务：** 串联一次截图、条件解码和 rotation 求值，输出条件值、命中条目、拟执行宏及键位；此阶段仅报告决策。

**完成标准：** 对可控输入能人工核对决策依据；多条满足时只选择首条，全部不满足时无动作；本步不会发送游戏按键。

### 13. TUI 展示条件与决策数据

- [x] 已完成：同帧条件、首条决策、宏名称日志与失效清空已验证（2026-09-13）。

**目标：** 在界面中解释当前 rotation 为什么选择某个动作。

**前置依赖：** 第 6、12 步。

**主要任务：** 展示当前 rotation、条件业务值、命中条目和拟执行动作，使同次决策的数据能够对应查看。

**完成标准：** 用户能从界面核对一次决策；显示结果与第 12 步一致；无命中时不会把上一轮动作显示成本轮结果。

**第 11–13 步验证（2026-09-13）：** 275 项 pytest、完整 mypy strict 与 Ruff 检查通过。新增白名单拒绝、类型检查、短路与 AST 复用、首条命中、Idle、无效截图和 TUI 日志／清空回归。
`demo/demo02.py` 真实 GDI 取得 `(20,36,3)` 基板，八项条件为 `0.0, 6, 2, False, 0.0, 0.0, 100.0, False`，命中第 3 条，拟执行宏“死神的抚摩”、键位 `CTRL-NUMPAD3`。
另以真实 GDI 和游戏检测连接 Textual `run_test`，验证相同条件与决策、日志窗口中的宏名称和单次试运行详情；停止清空决策，退出无残留 phantom 线程。
真实验证覆盖当前静态游戏帧；首条竞争、全部未命中及失败恢复由可控输入回归覆盖。未修改游戏插件、未发送按键、未实现 action；持续执行调度仍留到第 15 步。

2026-09-13 提交前复核修复 TUI 卸载期间刷新访问已移除控件的问题；新增两项退出回归后，277 项 pytest、完整严格类型检查和 Ruff 检查通过。本次未重复真实游戏验证。

## 第三阶段：完成真实动作闭环

### 14. 键盘基础模块

- [ ] 已实现并完成离线及 Windows 消息验证（2026-09-14）；真实游戏普通键、组合键和宏绑定验收待完成。

**目标：** 内核决定按键，键盘插件负责发送。

**前置依赖：** 第 3、7、10、12 步。

**已实现：** `core/keyboard` 校验 WoW 格式并冻结平台中立按键组合；`keyboards/liantian_cn.post_message@dev` 接收明确按键，在插件内查找唯一标题精确为“魔兽世界”的窗口。按用户 keyboard.py 参考依次按下、等待 10 ms、逆序释放，检查发送错误并尝试释放已按下的键。
生成器按用户 Macro.lua 标准示例，为 `bind_key=true` 输出安全按钮与覆盖绑定；`false` 不生成绑定。宏文本只进入 Lua 字符串，不传给键盘插件。

**完成标准：** 键位映射测试、Windows 消息及真实游戏普通/组合键验证；生成绑定符合配置，一次执行请求产生预期游戏动作。

**已验证边界：** 自动测试覆盖常用键位、非法配置、完整/失败消息顺序及绑定字符串转义；真实 PostMessageW 向测试自建隐藏窗口发送 CTRL-1 和 ALT-F4，收到对应按下/释放消息。未向真实游戏发送按键，未安装生成产物。

### 15. 单份 rotation 持续运行

- [ ] 已实现并完成离线闭环及 TUI 回归（2026-09-14）；真实游戏持续闭环待验。

**目标：** 将单帧决策连接为用户可控制的持续闭环。

**前置依赖：** 第 12、13、14 步。

**已确认并实现：** 后台按新帧运行，读取最新截图，每个序号最多决策并发送一次；相同内容的新帧仍处理，不补跑积压任务，频率上限沿用 `capture.fps`，默认 15。TUI 启动在有效 rotation 下同时执行，无 rotation 时仅采集；界面展示后台同帧决策。
`插件启用`、`爆发开启`、`正在延迟` 是内置布尔表达式变量，没有隐式发送门控。血 DK 示例首条规则将未启用或正在延迟匹配为 Idle。Idle 只跳过当前循环；Sleep/Pass 的未来别名不在本次实现。
窗口缺失/歧义、发送错误、意外执行错误暂停并要求手动重启；暂时无效截图只跳过本帧。关闭禁止新派发，并后台等待当前按键释放，界面继续响应。

**完成标准：** 真实游戏持续完成截图、解码、决策和按键；每轮至多一个组合，Idle 不发送；启停及异常符合上述语义。

**已验证边界：** 同帧决策、重复读取不重发、相同内容新帧、Idle 后恢复、错误后手动重启、停止等待释放、无积压发送及无残留线程均通过自动测试。无游戏测试环境，真实持续闭环未执行。

### 16. 截图模块插件化

- [ ] 插件化此前已实现；2026-09-14 补齐帧序号及离线持续闭环回归，真实游戏联合验收仍待完成。

**目标：** 保持既有截图精确版本边界并参与持续执行。

**前置依赖：** 公共核心与精确加载已提前实施；联合验收依赖第 15 步。

**已实现：** 保留 `core/capture`、`captures/liantian_cn.gdi@dev` 与默认 `capture.plugin`。`CaptureResult.sequence` 标识发布的新帧，快照复制保留值，公共 worker 跨重启递增；图像尺寸、RGB 与解码契约保持不变。不增加第二种真实截图后端或运行期热切换。

**完成标准：** 截图/解码契约一致，缺失版本明确拒绝，持续闭环回归通过。

**已验证边界：** 真实公共截图 worker 的定位、裁剪、序号、复制和重启测试通过；精确加载的合成截图插件与记录型键盘插件共同完成连续决策/发送回归。该离线测试不代替真实游戏联合验收。

### 17. 键盘模块插件化（提前实施）

- [ ] 已随第 14 步提前实现并离线验证（2026-09-14）；真实游戏联合验收待完成。

**目标：** 不同发送方式使用独立版本插件，内核统一传递明确按键。

**前置依赖：** 本次与第 14–16 步共同实施。

**已实现：** `actions` 预定分类改为 `keyboards`；公共 `send(KeyCombination)` / `close()` 契约和精确加载已接入。`keyboard.plugin` 默认 `liantian_cn.post_message@dev`，无版本回退；目标和设备编码由插件负责，不要求未来驱动或串口具有窗口目标。

**完成标准：** 精确选择、实例隔离、错误拒绝及键位语义一致，截图与键盘插件共同参与闭环；真实游戏结果验证完成。

**已验证边界：** 默认/显式配置、缺失版本、非法路径、加载和接口错误、键盘消息顺序、双插件离线闭环均已覆盖。不新增其他真实输入后端。

**第 14–17 步本轮验证：** 368 passed、5 skipped；完整 mypy strict（含所有条件、截图和键盘入口）通过。5 项跳过是本机无创建符号链接权限的既有路径逃逸测试。最终静态与格式检查记录见测试规范。用户已确认以实现及离线验收交付，以上未勾选项保留真实游戏验收边界。

## 第四阶段：扩展到日常可用

### 18. 多份 rotation 管理

- [ ] 待完成

**目标：** 从单份配置扩展为可管理多份配置的个人工具。

**前置依赖：** 第 10、13、15、16、17 步。

**主要任务：** 实现 TUI rotation 表格、首列复选框和包名输入；支持多选生成，同职业／专精互斥选择；按职业／专精匹配运行的一份 rotation。

**完成标准：** 不同职业／专精可共同生成一个包，相同组合的选择互斥；每份拥有对应 UUID Lua；同一时刻只运行一份，切换专精后沿用 `/reload` 规则。

**阶段设计问题：** 缺少匹配配置等交互在本步确认；本阶段不包含多窗口并发或天赋自动路由。

### 19. 扩充业务插件与示例循环

- [ ] 待完成

**目标：** 围绕实际使用场景逐批提高可用性。

**前置依赖：** 第 9、18 步。

**主要任务：** 根据用户实际职业／专精与循环需求选择插件，每批补齐版本契约、Lua/Python 编解码、参数与兜底说明，并提供可审阅的示例 rotation。

**完成标准：** 每批插件有算法测试和目标环境验证记录，示例能走通闭环；新版本与旧版本按精确标识共存，已测试配置不被自动升级。

**阶段设计问题：** 插件类别、清单及批次由实际需求确认；新增截图或行为后端只有在明确需要时再安排。

### 20. 完整验收与个人使用说明

- [ ] 待完成

**目标：** 得到能够按说明重复启动和使用的个人完整版本。

**前置依赖：** 第 1–19 步；第 19 步以当时确认的插件批次为边界，不要求穷尽所有插件。

**主要任务：** 完整验证配置加载、生成安装、游戏加载、截图、解码、决策、动作、TUI 操作及多 rotation 匹配；在用户说明中记录安装依赖、启动与停止、重新生成／重载、验证方法和已知限制。

**完成标准：** 能依据说明从配置走到真实运行；纯逻辑测试与类型检查通过；Windows 和游戏内验收分别记录环境及结果；未验证项明确列出，不把容器测试描述为真实闭环已通过。

**阶段边界：** 面向个人使用，暂不加入面向他人的分发包、自动更新或发布工程。

## 验证与维护原则

- 纯算法和业务规则测试随对应步骤完成，重点遵循 [测试规范](.spec/testing.md)，不集中拖到最后。
- Windows GDI、PostMessage 和游戏内行为必须在目标环境验证；容器负责可验证的配置、表达式、布局、编解码及生成产物检查。
- 每步完成时记录实际验证证据，再更新勾选状态；如需求变化，先调整受影响里程碑和依赖。
- 第 6 步验收数据采集可见，第 12 步验收单次决策，第 15 步验收真实持续闭环，第 20 步验收整体个人可用性。
